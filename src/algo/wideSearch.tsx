const search_queue = []
export const wideSearch = (cells, startCell, finishCell) => {
  
}

const getUnvisitedNeighbors = () => {

}

const getQueue = (cells) => {
  for(const cell of cells){
    search_queue.push(cell)
  }
}